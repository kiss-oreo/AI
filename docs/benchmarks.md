# Benchmarks

**Empty on purpose.** No measurement has been taken yet. Do not quote performance
numbers anywhere in the product or docs until a row exists here.

Template — one row per (machine, profile) pair:

| Date | Machine (CPU / RAM / GPU / disk) | Profile | UI startup s | Android boot s | APK install s | App launch s | QEMU RSS MB | UI RSS MB | CPU idle % | CPU load % | FPS | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
