# PortableDroid.Tests

Placeholder. xunit project, created at M3. First tests: PathService relativity,
QEMU argument builder (must never emit -snapshot), settings.json contains no
absolute drive letters, APK manifest parsing.
